{
  "description": "goal-kick：会话启动时确保进球监控守护进程存活",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/ensure-poller.sh"
          }
        ]
      }
    ]
  }
}
